{
  "name": "novaforge-engine",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "build:web": "rm -rf dist && tsc -p tsconfig.json && mkdir -p dist/styles && cp styles/ui.css dist/styles/ui.css && sed 's#./dist/app/main.js#./app/main.js#' index.html > dist/index.html",
    "vercel-build": "npm run build:web"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
